[tool.poetry]
name = "spanish-coach"
version = "0.1.0"
description = "A Spanish language learning agent built with Google ADK"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
google-adk = "^0.1.0"
google-generativeai = "^0.8.5"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api" 